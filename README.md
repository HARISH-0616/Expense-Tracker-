# Expense-Tracker-
A full-stack CRUD Expense Tracker built per the SOP: React frontend, Django REST Framework backend, SQLite database (swappable to PostgreSQL/MySQL), REST API, validation, and tests.
